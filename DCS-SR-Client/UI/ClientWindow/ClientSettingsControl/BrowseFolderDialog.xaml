<Window x:Class="Ciribob.DCS.SimpleRadio.Standalone.Client.UI.ClientWindow.ClientSettingsControl.BrowseFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:p="clr-namespace:Ciribob.DCS.SimpleRadio.Standalone.Client.Properties"
        Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="White"
        FontFamily="Segoe UI"
        FontSize="12">
    <Border BorderBrush="#FFCCCCCC" BorderThickness="1" CornerRadius="6" Background="White">
        <StackPanel>
            <!-- Custom Title Bar -->
            <Grid Height="32" Background="#CC0078D7">
                <TextBlock Text="{x:Static p:Resources.SelectRecordingDirectory}"
                           VerticalAlignment="Center"
                           Foreground="White"
                           FontWeight="Bold"
                           FontSize="15"
                           Margin="12,0,0,0"/>
                <Button Content="✕"
                        Width="32" Height="32"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        FontSize="14"
                        Click="Cancel_Click"/>
            </Grid>
            <!-- Dialog Content -->
            <StackPanel Margin="10">
                <TextBlock FontSize="12" Text="{x:Static p:Resources.currentSetRecordingDirectory}" Margin="0,0,0,5"/>
                <DockPanel>
                    <TextBox x:Name="PathTextBox" Width="280" Margin="0,0,5,0"/>
                    <Button Content="{x:Static p:Resources.Browse}" Width="80" Click="Browse_Click"/>
                </DockPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="{x:Static p:Resources.SetSRSRecordingPathBtn}" Width="80" Margin="0,0,5,0" Click="Ok_Click"/>
                    <Button Content="{x:Static p:Resources.BtnClose}" Width="80" Click="Cancel_Click"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>